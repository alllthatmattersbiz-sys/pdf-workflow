# PDF Workflow

Extract, process, and organize PDFs with Claude API.

## Setup

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Run

```bash
streamlit run app.py
```
